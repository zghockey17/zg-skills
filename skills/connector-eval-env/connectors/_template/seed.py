"""Placeholder seed. Replace with what your app needs before a session.

Real value: create the users listed in adapter.json, mark them onboarded and
allowed to log in, and pin any model-selection settings so every pipeline
call resolves to the model you mean to test. The `seed` hook in adapter.py
runs this (or a framework command); the engine never calls this file directly.
"""
if __name__ == "__main__":
    print("seed_ok=0 placeholder: replace seed.py")
